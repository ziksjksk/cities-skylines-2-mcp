{
  "name": "cs2-mcp",
  "version": "0.9.0",
  "description": "MCP server for Cities: Skylines II - bridges Claude to the CS2MCP in-game mod",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "cs2-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test ../tests/*.test.mjs",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "dotenv": "^17.4.2",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
